#include <bits/stdc++.h>

using namespace std;

#define rep(i,a,b) for(int i=a; i<=b; i++)

int main(){
    int n;
    string s;
    cin >> n >> s;
    rep(i,1,n)
        cout << "Minh yeu " << s << " nhieu lam!" << endl;
    return 0;
}
